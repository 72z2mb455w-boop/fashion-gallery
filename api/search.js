export default async function handler(req,res){res.setHeader("Access-Control-Allow-Origin","*");res.setHeader("Access-Control-Allow-Methods","POST,OPTIONS");res.setHeader("Access-Control-Allow-Headers","Content-Type");if(req.method==="OPTIONS")return res.status(200).end();if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});const{prompt}=req.body||{};if(!prompt)return res.status(400).json({error:"No prompt"});const apiKey=process.env.ANTHROPIC_API_KEY;if(!apiKey)return res.status(500).json({error:"ANTHROPIC_API_KEY not set"});try{const messages=[{role:"user",content:prompt}];let finalText="";for(let i=0;i<10;i++){const r=await fetch("https://api.anthropic.com/v1/messages",{method:"POST",headers:{"Content-Type":"application/json","x-api-key":apiKey,"anthropic-version":"2023-06-01"},body:JSON.stringify({model:"claude-sonnet-4-6",max_tokens:4000,tools:[{type:"web_search_20250305",name:"web_search"}],system:"You are a fashion product researcher. Search the web for real products matching the request. Return ONLY a raw JSON array where each item has: brand, name, price, imageUrl (direct CDN image URL ending in .jpg .jpeg .png or .webp), productUrl. Find 4-6 products per brand. Use Farfetch, SSENSE, Net-a-Porter, Mr Porter, END, Selfridges or official brand sites. Output ONLY the JSON array starting with [",messages})});const data=await r.json();if(data.error)throw new Error(data.error.message);messages.push({role:"assistant",content:data.content});if(data.stop_reason==="end_turn"){finalText=(data.content||[]).filter(b=>b.type==="text").map(b=>b.text).join("\n");break;}if(data.stop_reason==="tool_use"){const results=(data.content||[]).filter(b=>b.type==="tool_use").map(b=>({type:"tool_result",tool_use_id:b.id,content:"Search completed."}));messages.push({role:"user",content:results});}else{finalText=(data.content||[]).filter(b=>b.type==="text").map(b=>b.text).join("\n");break;}}let products=parseJson(finalText);if(!products){const r2=await fetch("https://api.anthropic.com/v1/messages",{method:"POST",headers:{"Content-Type":"application/json","x-api-key":apiKey,"anthropic-version":"2023-06-01"},body:JSON.stringify({model:"claude-sonnet-4-6",max_tokens:4000,messages:[{role:"user",content:"Extract fashion products and return ONLY a JSON array with fields: brand,name,price,imageUrl,productUrl.\n\n"+finalText.slice(0,8000)+"\n\nJSON array only:"}]})});const d2=await r2.json();products=parseJson((d2.content||[]).filter(b=>b.type==="text").map(b=>b.text).join("\n"));}if(!products)return res.status(422).json({error:"No products found."});res.json({products:products.filter(p=>p&&p.name&&p.brand)});}catch(e){console.error(e);res.status(500).json({error:e.message});}}function parseJson(text){if(!text)return null;try{const r=JSON.parse(text.trim());if(Array.isArray(r))return r;}catch{}const fenced=text.match(/```(?:json)?\s*([\s\S]*?)```/);if(fenced){try{const r=JSON.parse(fenced[1].trim());if(Array.isArray(r))return r;}catch{}}const s=text.indexOf("["),e=text.lastIndexOf("]");if(s!==-1&&e>s){try{const r=JSON.parse(text.slice(s,e+1));if(Array.isArray(r))return r;}catch{}}return null;}
